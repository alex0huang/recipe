#include 'dish.h'
using namespace std
class Dish {
public:
	Dish() {
		name = "name"
		ingredientCount = 0;
	}
	Dish(string name) {
		name = "name"
		ingredientCount = 0;
	}
	void addIngredient(cons) {
	
	}

private:
	string name;
	int p = maxingredientshow;
	Ingredient ingredients[p];
	int ingredientCount;
};